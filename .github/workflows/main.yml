name: job-example-1

on: [push]
jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - run: ls -la
      
  my-second-job:
    needs: my-first-job
    runs-on: ubuntu-latest
    steps:
      - run: ls -la
  
  my-third-job:
    if: github.repository == 'sameddemet/challenge-s10-e02'
    needs: [my-first-job, my-second-job]
    runs-on: ubuntu-latest
    steps:
      - run: ls -la